// import { createContext } from "react";

// type TTable = {
//   [record in string]: string | any;
// };

// type SHeesh = TTable;
// //   ^?

// const TableContext = createContext<TTable | null>({} as TTable);

// function Table({ columns, children }: TTable) {
//   return (
//     <>
//       <TableContext.Provider value={{ columns }}>
//         {children}
//       </TableContext.Provider>
//     </>
//   );
// }
